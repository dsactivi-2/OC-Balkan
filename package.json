{
  "name": "openclaw-balkan-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Vite-compatible landing page project for OpenClaw Balkan.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve:static": "python3 -m http.server 4173",
    "start": "node server.js",
    "start:prod": "NODE_ENV=production node server.js",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "express": "^5.2.1"
  }
}
